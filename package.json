{
  "name": "ogmcp",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "test:bridge": "cd bridge && go vet ./... && go test ./...",
    "lint:seams": "node scripts/lint-seams.mjs"
  },
  "devDependencies": {
    "dependency-cruiser": "^18.4.0",
    "typescript": "^5.9.3"
  }
}
